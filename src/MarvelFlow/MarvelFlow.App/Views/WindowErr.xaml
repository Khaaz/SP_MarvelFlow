<Window x:Class="MarvelFlow.App.Views.WindowErr"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MarvelFlow.App.Views"
        xmlns:vs="clr-namespace:MarvelFlow.App.ViewModels"
        mc:Ignorable="d"
        Title="WindowErr" Height="450" Width="800"
        Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1"
                   Text="{Binding TypeError}"
                   TextWrapping="Wrap" Foreground="White">
        </TextBlock>
        <TextBlock Grid.Row="2"
                   Text="{Binding TextError}"
                   TextWrapping="Wrap" Foreground="White">
        </TextBlock>
    </Grid>
</Window>
